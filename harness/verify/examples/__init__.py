"""Example conjecture modules for harness.verify.falsify.run.

These are loaded by path via importlib (see harness.verify.falsify._load_module),
so this package init is only here to make the directory importable/discoverable
too (e.g. for tests that want `from harness.verify.examples import goldbach_small`).
"""
